# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# DRIFT runtime artifacts
reference_out.npz
bench_results.json

# Internal / making-of material — kept on disk, no longer published.
# NOTE: this only stops FUTURE tracking; these files still exist in prior
# git history. Purging them from the past needs a history rewrite + force-push.
# Exception: docs/benchmarks.md is the one public doc kept in the repo.
docs/*
!docs/benchmarks.md
!docs/manual.md
!docs/manual.ko.md
!docs/manual.zh.md
!docs/manual.ja.md
!docs/img/
docs/img/_build/
DRIFT-implementation-spec.md
.claude/

# OS
.DS_Store
